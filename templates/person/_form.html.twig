{% form_theme form 'bootstrap_4_layout.html.twig' %}

{{ form_start(form) }}
    {{ form_row(form.firstname) }}
    {{ form_row(form.lastname) }}
    {{ include('person/_collectionWidget_form.html.twig', {'collection': form.phones}) }}
    <br>
    {{ include('person/_collectionWidget_form.html.twig', {'collection': form.addresses}) }}
    <br>
    {% if isFormEnabled %}
        <button class="btn btn-primary btn-lg float-right">{{ button_label|default('Save') }}</button>
    {% endif %}
{{ form_end(form, {'render_rest': false}) }}
